# Banco EXCLUSIVO da suíte de testes. Copie para `.env.test` (que fica
# fora do Git) e ajuste usuário/senha conforme seu Postgres local.
#
# Nunca aponte para o banco de produção: a suíte cria e APAGA registros.
# O setup dos testes recusa qualquer banco que não seja local ou cujo
# nome não se identifique como de teste.
#
# Depois de criar o arquivo, rode uma vez:
#   npm run test:db:preparar

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sysbelt_test"
DIRECT_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sysbelt_test"

JWT_SECRET="segredo-local-de-teste"
